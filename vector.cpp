#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> arr;
    cout << arr.size();
    cout << arr.capacity();
    vector<int> brr(10, -3);//this vector gets intialized with -3, generally vectors are intialized with 0 in default
    
}